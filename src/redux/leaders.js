import * as ActionTypes from "./ActionType";


export const Leaders = (state = {
    errMess: null, 
    isLoading: true,
    leaders: []
}, action) => {
    switch (action.type) {
        default:
            return state
    }
}